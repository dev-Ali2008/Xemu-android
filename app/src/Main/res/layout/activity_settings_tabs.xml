<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
     xmlns:android="http://schemas.android.com/apk/res/android"
     xmlns:app="http://schemas.android.com/apk/res-auto"
     android:layout_height="match_parent"
     android:layout_width="match_parent"
     android:background="@color/xemu_background">

    <com.google.android.material.appbar.MaterialToolbar
         android:layout_height="?attr/actionBarSize"
         android:layout_width="match_parent"
         android:background="@color/xemu_surface"
         app:title="@string/settings_title"
         app:layout_constraintTop_toTopOf="parent"
         app:layout_constraintStart_toStartOf="parent"
         app:titleTextColor="@color/xemu_text"
         app:navigationIcon="@drawable/ic_arrow_back"
         app:layout_constraintEnd_toEndOf="parent"
         android:id="@+id/toolbar">

    </com.google.android.material.appbar.MaterialToolbar>

    <com.google.android.material.tabs.TabLayout
         android:layout_height="wrap_content"
         android:layout_width="match_parent"
         app:tabMode="fixed"
         app:tabIndicatorColor="@color/xemu_green"
         app:tabTextColor="@color/xemu_text_muted"
         app:layout_constraintTop_toBottomOf="@id/toolbar"
         app:tabSelectedTextColor="@color/xemu_green"
         android:background="@color/xemu_surface"
         app:tabIconTint="@color/xemu_green"
         app:layout_constraintStart_toStartOf="parent"
         app:tabGravity="fill"
         app:layout_constraintEnd_toEndOf="parent"
         app:tabIndicatorHeight="3dp"
         android:id="@+id/tab_layout">

    </com.google.android.material.tabs.TabLayout>

    <androidx.viewpager2.widget.ViewPager2
         android:layout_height="0dp"
         android:layout_width="0dp"
         android:background="@color/xemu_background"
         app:layout_constraintStart_toStartOf="parent"
         app:layout_constraintEnd_toEndOf="parent"
         app:layout_constraintBottom_toBottomOf="parent"
         app:layout_constraintTop_toBottomOf="@id/tab_layout"
         android:id="@+id/view_pager" />

</androidx.constraintlayout.widget.ConstraintLayout>